# MajorProjectDiseasePrediction

Project scaffold for disease prediction from images.

Structure created:
- `train/` and `test/` (already present)
- `src/` with `data`, `models`, `training`, `evaluation`, `utils`
- `notebooks/` (optional)
- `app/` (for future web app)

See `src/` for starter code.
